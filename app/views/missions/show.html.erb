<div class="card-pf">
  <div class="login-pf-header">
    <h1>
      <%= @mission.project_name.upcase %>

      <% if is_logged_in %>
        <%= link_to 'Remove', mission_path(@mission), method: :delete, data: { :confirm => "Are you sure?" }, class: "btn btn-danger pull-right" %>
      <% end %>
    </h1>
  </div>

  <div class="row">
    <div class="col-md-6">
      <% if is_logged_in %>
      <%= render 'form_new' %>
      <% else %>
      <%= render 'anon_show' %>
      <% end %>
    </div>
  </div>
</div>

<div class="card-pf">
  <div class="row">
    <div class="col-md-12">
      <h2>
        Assets
        <% if is_logged_in %>
          <%= link_to 'Assign assets', mission_prepare_assets_path(@mission) , class: "btn btn-default pull-right" %>
        <% end %>
      </h2>
      <%= render 'assets_list' %>
    </div>
  </div>
</div>
